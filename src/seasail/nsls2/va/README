create_db.py is a script to create sql file, which can be used to create 
a test database, "itemfinder", for the ChannelFinder Service. It assumes 
MySQL as underlying RDB, and contains 2 tables:
1. element, which is physics name of each element
2. property, all pv informations are included in this table

Steps to create itemfinder database:
1. python create_db.py lat_conf_table.txt > create_itemfinder_db.sql
2. mysql -u root -p < create_itemfinder_db.sql 

